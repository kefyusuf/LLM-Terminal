"""Theme and color-cycling helpers for the Textual UI.

This package used to contain the ``ObsidianConsole`` experimental
workspace (removed in commit after the architecture review). It now
holds only the theme registry used by the live app.
"""
